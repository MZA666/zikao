<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.AdminMapper">

    <select id="selectAll" resultType="com.example.entity.SysUser">
        select * from `sys_user`
        <where>
            <if test="name != null"> and name like concat('%', #{name}, '%')</if>
        </where>
        order by user_id desc
    </select>

    <select id="selectById" resultType="com.example.entity.SysUser">
        select * from `sys_user` where user_id = #{userId}
    </select>

    <delete id="deleteById">
        delete from `sys_user` where user_id = #{userId}
    </delete>

    <insert id="insert" parameterType="com.example.entity.SysUser" useGeneratedKeys="true">
        insert into `sys_user`(user_id, username, password, name, create_time, role_id,status)
        values (#{userId}, #{username}, #{password}, #{name}, #{createTime}, #{roleId},#{status})
    </insert>

    <update id="updateById" parameterType="com.example.entity.SysUser">
        update `sys_user` set username = #{username}, password = #{password}, name = #{name}, avatar = #{avatar}, role_id = #{roleId}
        where user_id = #{userId}
    </update>

</mapper>