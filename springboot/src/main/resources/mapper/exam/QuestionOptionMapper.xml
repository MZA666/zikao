<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.exam.QuestionOptionMapper">

    <resultMap id="BaseResultMap" type="com.example.entity.exam.QuestionOption">
        <id column="id" property="id"/>
        <result column="question_id" property="questionId"/>
        <result column="option_key" property="optionKey"/>
        <result column="content" property="content"/>
        <result column="is_correct" property="isCorrect"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="created_time" property="createdTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, question_id, option_key, content, is_correct, sort_order, created_time
    </sql>

    <insert id="insert" parameterType="com.example.entity.exam.QuestionOption" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO question_option (question_id, option_key, content, is_correct, sort_order, created_time)
        VALUES (#{questionId}, #{optionKey}, #{content}, #{isCorrect}, #{sortOrder}, NOW())
    </insert>

    <delete id="deleteByQuestionId" parameterType="java.lang.Integer">
        DELETE FROM question_option WHERE question_id = #{questionId}
    </delete>

    <update id="updateById" parameterType="com.example.entity.exam.QuestionOption">
        UPDATE question_option
        SET question_id = #{questionId},
            option_key = #{optionKey},
            content = #{content},
            is_correct = #{isCorrect},
            sort_order = #{sortOrder}
        WHERE id = #{id}
    </update>

    <select id="selectById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM question_option
        WHERE id = #{id}
    </select>

    <select id="selectByQuestionId" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM question_option
        WHERE question_id = #{questionId}
        ORDER BY sort_order ASC
    </select>
    
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO question_option (question_id, option_key, content, is_correct, sort_order, created_time)
        VALUES
        <foreach collection="options" item="option" separator=",">
            (#{option.questionId}, #{option.optionKey}, #{option.content}, #{option.isCorrect}, #{option.sortOrder}, NOW())
        </foreach>
    </insert>

</mapper>